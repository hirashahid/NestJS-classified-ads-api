export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum UserType {
  ADMIN = 'admin',
  MEMBER = 'member',
}

export enum RoleType {
  BUYER = 'buyer',
  SELLER = 'seller',
  INSPECTOR = 'inspector',
  SUPERADMIN = 'super admin',
}
